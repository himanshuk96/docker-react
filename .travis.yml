sudo: required
services:
  - docker


before_install:
  - docker build -t /home/radhekrishna/docker-react -f dockerfile.dev .


scripts:
  - docker run /home/radheskrishna/docker-react npm run test -- --coverage